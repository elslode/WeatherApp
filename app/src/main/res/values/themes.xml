<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Weather" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor"
            tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="day_of_week">
        <item name="android:textSize">@dimen/day_of_week</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="temperature">
        <item name="android:textSize">@dimen/temperature</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="internet_text">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/internet_text</item>
    </style>

    <style name="state_of_weather">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/state_weather</item>
    </style>
</resources>